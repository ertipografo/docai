import { Menu, X } from "lucide-react";

export default function Toolbar({ showSidebar, setShowSidebar }) {
  return (
    <div className="w-complementarySidebarWidth bg-bg1 border-r border-borderColor p-10 h-sidebarMaxHeight top-0 sticky">
      <div className="bg-rose-300">poppa</div>
    </div>
  );
}
